# TANSTACK QUERY MAKES THIS PROJECT INCOMPATIBLE WITH:
* Chrome >= 91
* Firefox >= 90
* Edge >= 91
* Safari >= 15
* iOS >= 15
* Opera >= 77

FIX

## DOCKER
```bash
docker build -t my-app .
docker run -p 3000:3000 my-app
```
